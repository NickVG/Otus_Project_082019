version: '3'
services:
  search_engine_ui:
    image: ingorbunovi/search_engine_ui:2.1
    environment:
      - MONGO=db
      - MONGO_PORT=27017
#        EXCLUDE_URLS=${EXCLUDE_URLS}
    ports:
      - "8000:8000"
    networks:
      - backend
      - frontend

networks:
  frontend:
  backend:

